  <% @posts.each do |post| %>
    <div class="blog-post">
      <h2 class="blog-post-title"><a href="<%= post_path id:post.id %>"><%= post.title %></a></h2>
      <p class="blog-post-meta"><%= local_time_format(post.created_at) %></p>
      <p><%= post.text %></p>
    </div>
  <% end %>

